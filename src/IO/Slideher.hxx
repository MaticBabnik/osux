#include "../include.hxx"
#include "Beatmap.hxx"

namespace IO {

    namespace SliderMath {
        long long factorial(int n);
        double binomial_coefficent(int n, int k);
        vector<SDL_Point> resolve_curve(vector<SDL_Point> input, SliderType st);
        //size_t bezier_curve(vector<OsuPoint> *cp,)
    }

    class TimePoints {

    };

    class Slideher {

    };

}